# The example the deploy the same static site to an AWS S3 Bucket and a DigitalOcean Space bucket

**Highlight**: a dynamic resource provider to specify the index page of a Space bucket, relying on the following APIs:

- PutWebsiteIndex
- DeleteWebsiteIndex
